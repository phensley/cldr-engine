import { SchemaConfig } from '@phensley/cldr-schema';

export const DEFAULT_CONFIG: SchemaConfig = {
  calendars: ['gregory'],
  names: [],
  currencies: ['USD', 'GBP', 'JPY'],
  units: ['foot', 'inch', 'mile']
};
