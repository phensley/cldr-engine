import { KeyIndex } from '../../types';

export const ListPatternPositionIndex = new KeyIndex(['start', 'middle', 'end', 'two']);

export type ListPatternPositionType = 'start' | 'middle' | 'end' | 'two';
