## API Report File for "@phensley/cldr-utils"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export const binarySearch: (elems: number[], lessThan: boolean, n: number) => number;

// @public
class Cache_2<T> {
    constructor(builder: (s: string) => T, capacity: number);
    get(raw: string): T;
    size(): number;
    }

export { Cache_2 as Cache }

// @public
export class Checksum {
    constructor();
    get(): number;
    update(s: string): this;
    }

// @public
export type Comparator<T> = (a: T, b: T) => number;

// @public
export const enum Constants {
    // (undocumented)
    FNV1A_BASIS = 2166136261
}

// @public
export const FNV: (r: number, s: string) => number;

// @public
export class Heap<T> {
    constructor(cmp: Comparator<T>, data: T[]);
    empty(): boolean;
    pop(): T | undefined;
    push(item: T): void;
    }

// @public
export type Key = string | number;

// @public
export class LRU<V> {
    constructor(capacity?: number);
    get(key: Key): V | undefined;
    set(key: Key, val: V): void;
    size(): number;
    toString(): string;
}

// @public
export const mphash: (d: number, len: number, s: string) => number;

// @public
export interface MPHashTable<T> {
    // (undocumented)
    g: number[];
    // (undocumented)
    s: number;
    // (undocumented)
    v: T[];
}

// @public
export const mplookup: <T>(t: MPHashTable<T>, key: string) => T;


// (No @packageDocumentation comment for this package)

```
